import 'package:flutter/material.dart';

class Travel2AzkarScreen extends StatelessWidget {
  const Travel2AzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text('Travel2AzkarScreen')),
        body: const Center(child:  Text('Travel2AzkarScreen')));
  }
}
