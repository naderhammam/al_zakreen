import 'package:flutter/material.dart';

class TravelAzkarScreen extends StatelessWidget {
  const TravelAzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('TravelAzkarScreen')),
        body: const Center(child: Text('TravelAzkarScreen')));
  }
}
