import 'package:flutter/material.dart';

class FoodAndDrinkAzkarScreen extends StatelessWidget {
  const FoodAndDrinkAzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(title: Text('FoodAndDrinkAzkarScreen')),
        body: const Center(child:  Text('FoodAndDrinkAzkarScreen')));
  }
}
