import 'package:flutter/material.dart';

class PrayAzkarScreen extends StatelessWidget {
  const PrayAzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(title: Text('PrayAzkarScreen')),
        body: const Center(child:  Text('FoodAndDrinkAzkarScreen')));
  }
}
