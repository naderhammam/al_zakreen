import 'package:flutter/material.dart';

class SleepAzkarScreen extends StatelessWidget {
  const SleepAzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('SleepAzkarScreen')),
        body: const Center(child: Text('FoodAndDrinkAzkarScreen')));
  }
}
