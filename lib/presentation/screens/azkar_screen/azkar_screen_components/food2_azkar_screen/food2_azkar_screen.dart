import 'package:flutter/material.dart';

class Food2AzkarScreen extends StatelessWidget {
  const Food2AzkarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Food2AzkarScreen')),
        body: const Center(child: Text('Food2AzkarScreen')));
  }
}
