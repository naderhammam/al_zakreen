import 'package:flutter/material.dart';

class ElNwayaScreen extends StatelessWidget {
  const ElNwayaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(title: const Text('ElNwaya')),
        body: const Center(child: Text('ElNwayaScreen')));
  }
}
